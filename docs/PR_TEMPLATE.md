# PR 説明のひな形

GitHub で Pull Request を作成すると、[`.github/pull_request_template.md`](../.github/pull_request_template.md) が本文に自動挿入されます。

CLI やエディタだけで下書きするときは、同じ見出し（関連・概要・変更内容など）に沿って書いてください。**マスタの文言は `.github` 側**とし、このファイルは参照用です。
