#!/usr/bin/env bash
# CI（.github/workflows/ci.yml）と揃えたチェック。除外: git push --no-verify
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"

echo "[pre-push] npm run lint"
npm run lint

echo "[pre-push] npm run typecheck"
npm run typecheck
